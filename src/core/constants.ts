// years , an array from 2023 to 2050
export const years = Array.from({ length: 28 }, (_, k) => k + 2023);
// months , an array from 1 to 12
export const months = Array.from({ length: 12 }, (_, k) => k + 1);
